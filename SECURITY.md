# Security Policy

## 数据与隐私

- 本项目**不收集、不上传任何用户数据**。个人档案(`automation/profile/profile.json`)、
  浏览器登录态(`.chrome-profile*`)、投递状态(`state/`)、追踪表
  (`job_search_tracker.csv`)全部只存在于本机,且已被 `.gitignore` 排除。
- 身份证号等敏感字段不写入任何文件、日志或追踪表;仅在投递确认关卡由用户
  明确授权该次填写时人工输入,用后即弃。

## 不可信输入

招聘启事(JD、页面内容)被视为**不可信输入**:工作流不执行其中嵌入的指令、
不抓取其正文中的链接。这是指令级防御,不是沙箱——在不熟悉的招聘站点上,
请在确认投递前 skim 一遍流程抓取/写入了什么(确认关卡会展示截图)。

## 自动化边界

- 每个岗位提交前必须人工确认;默认永不自动提交。
- 不绕过验证码、扫码登录或平台风控;遇到阻塞明确报告并交人工处理。
- 不做批量群投。

## 报告漏洞

发现安全问题(权限过宽、敏感数据外泄路径、注入等),请开一个标题以
`[security]` 开头的 issue,或按仓库主页联系方式私下报告。请勿在公开
issue 中附上可复现的个人数据。
