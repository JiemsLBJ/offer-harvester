{
  "name": "shixiseng-search-cli",
  "version": "1.0.0",
  "description": "CLI for searching 实习僧 (www.shixiseng.com) internship postings by keyword and city — Nuxt SSR pages, zero runtime dependencies, no login needed for reads.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "shixiseng-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 60000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
