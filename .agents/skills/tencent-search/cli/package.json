{
  "name": "tencent-search-cli",
  "version": "1.0.0",
  "description": "CLI for searching Tencent jobs (careers.tencent.com) via its public careers API — zero runtime dependencies, no login needed for reads.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "tencent-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 60000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
